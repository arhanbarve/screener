# Secrets
.env

# Application data
# positions.json is tracked so the cloud dashboard sees current positions

# Development
.superpowers/
.worktrees/

# Cache and generated data (too large / regenerated each run)
data/*
# except the small fidelity snapshot the cloud dashboard reads
!data/fidelity/
data/fidelity/*
!data/fidelity/positions_data.json
# and the alpaca paper snapshot the Paper page reads (no account identifiers)
!data/alpaca/
data/alpaca/*
!data/alpaca/portfolio.json
logs/

# Handoff docs (local-only, read via filesystem not git)
docs/handoffs/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# Node
node_modules/

# Claude Code local settings/plugins
.claude/

# Nested repo (cloned separately, not a submodule)
agent-skills/
