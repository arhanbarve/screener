<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.arhanbarve.watchdog</string>
    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>/Users/arhanbarve/Code/screener/scripts/watchdog.sh</string>
    </array>
    <key>RunAtLoad</key>
    <true/>
    <!--
      Every 30 minutes. The script itself decides what to check: the pytest run
      only happens on the 22:00 tick, so the routine ticks stay cheap. launchd
      fires a missed interval on wake, so closing the lid does not mean the
      watchdog silently stops.
    -->
    <key>StartInterval</key>
    <integer>1800</integer>
    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
    </dict>
    <key>StandardOutPath</key>
    <string>/Users/arhanbarve/Code/screener/logs/watchdog_launchd.log</string>
    <key>StandardErrorPath</key>
    <string>/Users/arhanbarve/Code/screener/logs/watchdog_launchd.log</string>
</dict>
</plist>
